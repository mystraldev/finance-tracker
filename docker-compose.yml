services:
  app:
    build:
      context: .
      target: build
    image: finance-tracker-dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    command: ["pnpm", "run", "dev", "--host"]
    environment:
      - NODE_ENV=development
